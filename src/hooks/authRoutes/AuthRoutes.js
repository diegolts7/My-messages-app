import axios from "axios";

const AuthRoutes = {
  async login(email, password, setflashMessage) {
    try {
      //axios.post("https://my-messages-api.vercel.app/auth/login", )
    } catch (error) {
      console.log(error);
    }
  },
};

export default AuthRoutes;
